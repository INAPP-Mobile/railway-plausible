{
  "BASE_URL": {
    "defaultValue": "",
    "description": "Public URL of your Plausible instance (e.g., https://plausible.yourdomain.com). Set after deploy in Variables tab.",
    "isOptional": true
  },
  "SECRET_KEY_BASE": {
    "defaultValue": "${{secret(64)}}",
    "description": "64-byte random string for cookie signing and secrets. Auto-generated.",
    "isOptional": false
  },
  "DATABASE_URL": {
    "defaultValue": "${{Postgres.DATABASE_URL}}",
    "description": "PostgreSQL connection string. Add a Railway Postgres service to auto-inject.",
    "isOptional": false
  },
  "CLICKHOUSE_DATABASE_URL": {
    "defaultValue": "http://${{clickhouse.CLICKHOUSE_USER}}:${{clickhouse.CLICKHOUSE_PASSWORD}}@clickhouse:8123/${{clickhouse.CLICKHOUSE_DB}}",
    "description": "ClickHouse connection string. References companion ClickHouse service credentials. Default works with Railway's ClickHouse companion service named 'clickhouse'.",
    "isOptional": true
  },
  "DISABLE_REGISTRATION": {
    "defaultValue": "false",
    "description": "Set to true to disable public registration after creating your admin account.",
    "isOptional": true
  },
  "ENABLE_EMAIL_VERIFICATION": {
    "defaultValue": "false",
    "description": "Set to true to require email verification for new users.",
    "isOptional": true
  }
}
